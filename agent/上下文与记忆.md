上下文的七大组成要素：

- System Instructions — 系统指令，定义角色和行为边界
- User Prompt — 用户的当前请求
- Conversation History — 对话历史
- Long-Term Memory — 跨会话的持久记忆
- Retrieved Information (RAG) — 实时检索的外部知识
- Available Tools — 可用工具的定义和 schema
- Output Format — 输出格式要求

每一项都在争夺上下文窗口这块有限的空间。上下文工程的核心挑战，就是在这七个维度之间做出最优分配。Agent 的失败，本质上是上下文的失败，而不是模型的失败。

Token 是 LLM 处理文本的最小单位——不是字符，也不是单词，而是模型内部切分文本的"碎片"。不同语言的 token 效率不同：

英文	~4 字符/token	按词根和常见词切分
中文	~1.5 字符/token	每个汉字约 1-2 token
代码	~3 字符/token	符号和关键字独立切分

这意味着同样的语义内容，中文消耗的 token 更多，可以根据这个估算 token，再加上 10% 的安全边际。

随着上下文中 token 数量增加，模型准确回忆和利用信息的能力会递减。

原因在 LLM 架构本身，自注意力（Self-Attention）的计算量与 token 数量呈 n² 关系：10K token 需要约 1 亿次注意力计算，50K token 就是约 25 亿次。计算量的暴增创造的不是一个"硬悬崖"，而是一个性能梯度——信息检索的准确率随着上下文长度逐渐下滑。

核心结论：上下文是有限资源，具有递减的边际回报。往里塞更多信息，不一定能让模型表现更好。

上下文工程的四项策略：

- Write 写出：把信息写入到外部存储，无限记忆
- Select 检索：Just-In-Time 即时检索，预加载 + 按需混合
- Compress 压缩：摘要替代原文，三段式保留策略，工具结果清除
- Isolate 隔离：拆分到子 Agent

**write 写出**

Write 策略的核心是：让 Agent 把信息主动写出到外部存储，需要时再读回来。

最典型的实践是 Scratchpad（便签本）模式。Agent 在执行复杂任务时，维护一个 todo.md 或 NOTES.md 文件，把任务目标、已完成步骤、待解决问题写进去。这么做有两个好处：

第一，避免 "Lost-in-the-Middle" 问题。研究表明，LLM 对上下文中间位置的信息关注度最低。把关键信息写到文件里，下次需要时读回到上下文末尾——正好落在模型注意力最强的区域。

第二，文件系统即无限记忆。上下文窗口是有限的"RAM"，但文件系统是无限的"硬盘"。Agent 学会按需写入和读取文件，本质上是在用硬盘扩展 RAM。

许多 AI 编程助手已经在实践这个策略。它们维护待办列表来跟踪任务进度，使用项目级配置文件来持久化关键上下文——本质上都是 Write 策略的实践：把持久化信息写到上下文外部，腾出窗口空间给真正需要推理的内容。

另一个关键实践是目标复述：让 Agent 在上下文的末尾重新复述当前的全局目标和计划。这不是浪费 token——这是在操控模型的注意力机制，确保它始终"记得"自己在干什么。

**Select 相关信息检索**

Write 把信息写出去了，Select 负责在正确的时刻把正确的信息检索回来。

核心原则是 Just-in-Time（即时）上下文——不是把所有信息预加载到上下文里，而是在需要的时候才去取。

实践中最有效的是混合策略：预加载关键信息，把最核心的、每次都需要的信息放在 System Prompt 里；按需检索详细内容，具体的代码、文档、数据通过工具（glob、grep、RAG）在需要时才拉入上下文。

现代 AI 编程助手完美体现了这种混合策略：项目配置文件提供预加载的上下文（比如代码规范、架构决策），文件搜索和代码检索工具提供即时检索能力。Agent 不需要"看过所有代码"，只需要知道"在哪里找"。

这背后是一种叫**渐进式披露（Progressive Disclosure）**的设计理念：Agent 通过探索逐步发现上下文，逐层组装理解。文件大小暗示复杂度、命名暗示用途、时间戳暗示相关性——Agent 像侦探一样，从线索出发，逐步拼出全貌。

System Prompt 的设计需要注意 Goldilocks Zone（适中区间）。过度具体意味着脆弱——场景稍变就失效。过度模糊意味着信号不足——模型不知道该做什么。最优的 System Prompt 是足够具体以引导行为，又足够灵活以适应变化。

**Compress 压缩上下文**

当对话越来越长，上下文窗口逐渐被填满时，就需要压缩了。

压缩的核心操作叫 Compaction（压实）：用 LLM 把冗长的对话历史压缩成精炼的摘要，然后用摘要替代原始内容，重新初始化上下文窗口。

Shannon 实现了一种经过实战验证的三段式保留策略：

- Primers（前 3 条）：保留对话开头。用户最初需求、系统设定都在这里建立。如果丢了，Agent 可能给出完全不相关的建议。
- Summary（中间摘要）：用 LLM 把中间几百条消息压缩成一段语义摘要。保留关键决策、重要发现和未解决问题。
- Recents（后 20 条）：保留最近的对话，维持连贯性。用户说"刚才那个方案"，Agent 能在 Recents 里找到。

什么时候触发压缩？不是每次都压缩——那样太浪费计算资源。Shannon 的策略是：预算使用率达到约 75% 时触发，目标压到约 37.5%。比如预算是 50K tokens，用到 37.5K 就开始压缩，压到 18.75K 左右。75% 留 25% 余量给当前轮的输入输出，37.5% 压到一半以下给后续对话留空间。

摘要生成是压缩中最慢的操作（200-500ms），但只在触发压缩时才跑，不是每次请求都跑。

压缩是有损的——这必须承认。那么保留什么、丢弃什么？

优先保留的：架构决策和关键结论、未解决的 bug 和待处理事项、核心实现细节和文件路径。可以丢弃的：冗余的工具输出（大段的 JSON 返回值）、重复的试错消息、确认性寒暄。

其中工具结果清除是最安全的压缩形式——一段 5000 token 的 API 返回值，Agent 已经从中提取了有用信息，原始数据就可以清除了。

一个重要原则：可恢复压缩。压缩时保留 URL 和文件路径，不做不可逆丢弃。这样即使摘要丢失了细节，Agent 还能重新读取原始源。

还有一个反直觉的实践：保留错误上下文。不要清除 Agent 的失败尝试——这些错误是宝贵的学习信号。模型看到之前的失败路径，会隐式更新内部信念，避免重蹈覆辙。擦除失败记录等于擦除了经验。

**上下文隔离**

当一个任务太复杂、一个上下文窗口装不下时，拆分。

Isolate 策略的核心是 Sub-Agent Architecture（子 Agent 架构）：把任务分解给专门的子 Agent，每个子 Agent 在自己干净的上下文窗口中工作，只把精华结果返回给主 Agent。

这带来了极大的信息压缩比。一个子 Agent 可能在其窗口中探索了数万 token 的信息——读代码、搜文档、试方案——但最终只返回 1000-2000 token 的精华摘要给主 Agent。

三个有效的隔离场景：

上下文隔离：子任务会产生大量中间数据（比如搜索结果、调试日志），但主任务只需要最终结论。
并行化：多个子 Agent 同时探索不同方向，各自在独立窗口中工作，互不干扰。
专业化：当工具定义超过 20 个时，每个工具定义占 200+ token，光工具就吃掉 4000+ token 的上下文。拆分成每个子 Agent 最多 5 个工具，让它们各自专精。
Token 预算在隔离架构中也需要分层管理。Shannon 实现了 Session → Task → Agent 三级预算。


### 记忆系统

上下文窗口是有限的，因为自注意力的机制，会话越多，效果越低，因此需要对历史会话进行管理，就产生了记忆。历史会话、工具调用、知识检索

要让 Agent「记得住做过什么」，需要一套分级记忆。这也是「核心组件」里记忆模块的落地。Agent 中的记忆，通常围绕两个维度展开：**短期/长期**（时间尺度）与**情景/语义/经验**（内容类型）。

**短期记忆（Short-term）**：也叫做会话记忆，当前对话的上下文。会话变长时，Agent 会对历史消息做摘要/压缩，只保留重要信息重新放入 context。

**长期记忆（Long-term）**：解决"当前会话结束、下次能否记住"的问题。信息不能只放 context，需要**持久化存储**，可分为事实记忆 / 情景记忆 / 经验记忆。

**语义记忆**：永久，这部分其实就是RAG了。


短期记忆就是上面的上下文四种策略。

Redis：热数据，正在进行的会话，毫秒级访问。

- 活跃会话：减少数据库查询
- Token 预算：实时追踪，需要原子操作
- 速率限制：高频读写

长期记忆：

第一个要保存完整的回话历史：
- 执行历史：哪个 Agent 做了什么、成功还是失败
- 审计日志：谁在什么时候做了什么操作
- 用户偏好：显式保存的配置

语义记忆：

- 语义记忆：找"意思相近"的历史
- 压缩摘要：上一章讲的摘要存储
- 文档分块：RAG 场景的知识库

**分层记忆检索**

```
用户问: "继续上次的讨论"
              │
              ├── Recent (最近 5 条)
              │   └── 保持对话连贯
              │
              ├── Semantic (语义相关 3 条)
              │   └── 找历史中相关的
              │
              └── Summary (摘要 2 条)
                  └── 快速了解长期上下文
```

### 多轮会话设计




**短期记忆如何存储？**
短期记忆本质上是保存当前任务所需上下文与状态，而非永久保存所有历史。
- 存储在 Redis
- 若 Agent 执行时间很长，采用 数据库 + Redis 存储

注意：短期记忆 ≠ 把 message 一直保存。全部塞给 LLM 会出现上下文膨胀，因此通常要做 **Token 检测**，超过阈值就压缩/摘要，得到「Summary + 最近消息」再放入 Context。

最终的 Context 可能是：

```text
┌──────────────────────────┐
│ System Prompt            │
├──────────────────────────┤
│ Conversation Summary     │ ← 压缩后的历史
├──────────────────────────┤
│ Recent Messages          │ ← 最近 N 条
├──────────────────────────┤
│ Relevant Memories        │ ← 从长期记忆检索
├──────────────────────────┤
│ Current Agent State      │ ← 当前任务状态
└──────────────────────────┘
```

**短期记忆应该在何时转成长期记忆？**
- 当用户明确要求"立即记忆"
- 在任务结束后记忆
- 在 context 压缩时记忆

短期转长期的过程：

```text
短期记忆 Conversation / Working Memory
        ↓
   重要性判断
        ↓
   信息抽取
        ↓
去重 / 冲突检测
        ↓
结构化 & Embedding
        ↓
长期记忆 Semantic（语义、事实） / Episodic（情景） / Procedural（程序、经验）

分别对应：知道什么？发生过什么？以后怎么做？
```

**分级记忆体系（存储选型）：**

- **工作记忆**：记录会话消息，生命周期与单个会话绑定，会话结束后自动清理，采用内存存储，TTL 管理。
- **情景记忆**：长期存储具体的交互事件和学习经历，使用 SQLite + Qdrant。
- **事实/语义记忆**：存储更抽象的知识、概念和规则，使用 Qdrant + Neo4j。
- **感知记忆**：专门处理图像、音频等多模态信息，支持跨模态检索，使用 SQLite + Qdrant。

> 走到这里，一个「会思考、能看上下文、能行动、记得住」的 Agent 已经成型。接下来看它如何融入真实的生产环境和生态。


想象一个真实流程：

当多轮会话的大小，达到上下文窗口的75%，触发一次上下文压缩。

当会话结束之后，会话内容转为长期记忆，使用定时任务？

消息历史管理：对话可能有几百轮。不能全存，要设上限，maxHistory 通常设 500。超了就把最老的裁掉。

为什么是 500？

太小（比如 50）：历史太短，上下文不够
太大（比如 5000）：Redis 存储压力大，每次加载慢
500 是个平衡点。一般对话不会超过 500 轮。


MMR（Maximal Marginal Relevance）平衡相关性和多样性：

上下文（Context）解决“这一次任务，我现在需要知道什么？”
记忆（Memory）解决“跨任务、跨会话，未来还需要知道什么？”

```
                    ┌─────────────────────┐
                    │      User Input     │
                    └──────────┬──────────┘
                               ↓
                    ┌─────────────────────┐
                    │ Context Orchestrator│
                    └──────────┬──────────┘
                               ↓
        ┌──────────────────────┼──────────────────────┐
        ↓                      ↓                      ↓
  当前对话 Context         长期 Memory            外部知识/RAG
        ↓                      ↓                      ↓
  最近消息/任务状态      用户偏好/历史事实       文档/数据库/API
        └──────────────────────┼──────────────────────┘
                               ↓
                    ┌─────────────────────┐
                    │   Prompt / State    │
                    └──────────┬──────────┘
                               ↓
                    ┌─────────────────────┐
                    │       LLM/Agent     │
                    └──────────┬──────────┘
                               ↓
                  ┌────────────────────────┐
                  │ Action / Tool / Reply  │
                  └────────────────────────┘
```

记忆是上下文管理的一部分。

记忆分为短期记忆和长期记忆，短期记忆就是当前的会话的内容，长期记忆可以跨越多个任务和会话。

这样会带来三个问题：
- Token 成本越来越高
- 重要信息被大量旧信息淹没
- 模型容易受到过时信息影响
更合理的方法是把 Context 分层。

System Context
```
Agent 身份
行为规则
安全规则
工具定义
输出格式
```

Task Context
```
用户当前目标
任务状态
已经完成的步骤
待完成步骤
当前中间结果
```

Recent Conversation 只保留最近N论
```
User: ...
Agent: ...
User: ...
Agent: ...
User: ...
```

Context Compression

会话历史：这一部分是事实数据，应该不做修改单独保存，便于后期查看、进行审计。

工作记忆（任务状态）：对于 Agent 场景，需要维护当前任务状态、工具调用栈、计划步骤等。可存入 sessions.metadata（JSONB）或单独的 task_states 表，避免频繁读写主库，任务目标是什么，计划步骤是什么，进行到哪一步。

上下文摘要：应该单独保存，不要只保存最新 Summary，最好保留压缩版本和生成依据。
```
Context Summary
├── 1. 当前目标 Goal
├── 2. 已确定的事实 Facts
├── 3. 用户的要求 / Constraints
├── 4. 已做出的决定 Decisions
├── 5. 当前任务进展 Progress
├── 6. 尚未解决的问题 Open Issues
├── 7. 重要的上下文 / 背景 Background
├── 8. 已产生的结果 / Artifact
└── 9. 必须保留的引用信息 References
```

```
{
  "summary_version": 3,

  "goal": "...",

  "background": [
    "..."
  ],

  "facts": [
    "..."
  ],

  "constraints": [
    "..."
  ],

  "decisions": [
    "..."
  ],

  "progress": {
    "completed": [],
    "current": "...",
    "next": []
  },

  "open_issues": [
    "..."
  ],

  "artifacts": [
    {
      "id": "...",
      "description": "..."
    }
  ],

  "references": [
    {
      "message_start": 1,
      "message_end": 100
    }
  ]
}
```


长期记忆的触发：

- 对话过程中发现“明确记忆”——立即提取
```
我叫 Alex。
我是一名 Java 工程师。
我不喜欢使用 MongoDB。
以后代码都用 TypeScript。
```

- Summary 压缩时——做一次批量 Memory Extraction
这里和summary的提示词是不一样的
Summary：“这段对话发生了什么？”
Memory Extraction：“这段对话中，有什么信息值得未来的 Agent 永久/长期记住？”

- Conversation 结束时——做一次 Memory Consolidation


一个更成熟的设计：Memory Candidate

```
Summary / Messages
       ↓
Memory Candidate
       ↓
Memory Evaluator
       ↓
Long-term Memory
```

```
                Candidate
                    │
          ┌─────────┴─────────┐
          │                   │
       important?          confidence?
          │                   │
          └─────────┬─────────┘
                    ▼
               Memory Store
```

长期 Memory 不一定“永久”，记忆的修改和删除

当新信息与旧 Memory 冲突时，Memory 系统如何判断“Lucy 替代了 Alex”，而不是把 Alex 和 Lucy 都保存下来？

我建议把长期记忆设计成 “事实 + 版本 + 状态”，并且对某些 Memory Type 建立 唯一约束（identity key）。

```
{
  "user_id": "user_001",

  "type": "profile",
  "key": "name",

  "content": "用户的名字是 Alex",

  "value": {
    "name": "Alex"
  },

  "status": "active",

  "version": 1
}
```

```
{
  "id": "mem_002",

  "user_id": "user_001",

  "type": "profile",

  "key": "name",

  "content": "用户的名字是 Lucy",

  "value": {
    "name": "Lucy"
  },

  "confidence": 0.99,

  "status": "active",

  "version": 2,

  "supersedes_id": "mem_001",

  "source": {
    "conversation_id": "conv_009",
    "message_id": "msg_103"
  },

  "created_at": "...",
  "updated_at": "..."
}
```

更推荐维护一个受控的 Type Registry。

千问

用户画像 USER.md
长期记忆 MEMORY.md
短期记忆 memory/
索引

```
Message
   ↓
Memory Extraction
   ↓
Candidate
   ↓
Evaluation
   ↓
Existing Memory Search
   ↓
┌─────────────┐
│ New         │
│ Update      │
│ Merge       │
│ Ignore      │
│ Delete      │
└─────────────┘
```
